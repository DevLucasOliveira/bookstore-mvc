<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>_LayoutNew</title>
    <link rel="stylesheet" href="https://bootswatch.com/4/pulse/bootstrap.min.css" medial="all"/>
</head>
<body>
    @if (IsSectionDefined("menu"))
    {
        <div class="col-md-4">
            <h3>Menu</h3>
            @RenderSection("menu", false)
        </div>
    }

    <div class="col-md-4">
        <h3>Menu</h3>
        @RenderSection("menu", false);
    </div>
    <div class="container well col-md-8"> 
        @RenderBody()
    </div>

    @RenderSection("Scripts", false);
</body>
</html>
